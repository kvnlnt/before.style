import type { DocumentedComponent } from "../types";

export const feed: DocumentedComponent = {
  title: "Feed",
  description:
    "A scrollable list of articles where new content may be added as the user scrolls.",
};
