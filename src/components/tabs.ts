import type { DocumentedComponent } from "../types";

export const tabs: DocumentedComponent = {
  title: "Tabs",
  description:
    "A set of layered content panels where only one panel is displayed at a time.",
};
