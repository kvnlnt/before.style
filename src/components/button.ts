import type { DocumentedComponent } from "../types";

export const button: DocumentedComponent = {
  title: "Button",
  description:
    "An interactive element that triggers an action when activated by the user.",
};
