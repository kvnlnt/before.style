import type { DocumentedComponent } from "../types";

export const dialog: DocumentedComponent = {
  title: "Dialog",
  description:
    "A modal or non-modal window that appears above the page content requiring user interaction.",
};
