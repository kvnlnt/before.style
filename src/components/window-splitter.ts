import type { DocumentedComponent } from "../types";

export const windowSplitter: DocumentedComponent = {
  title: "Window Splitter",
  description:
    "A movable divider that separates two sections and allows resizing them.",
};
