import type { DocumentedComponent } from "../types";

export const listbox: DocumentedComponent = {
  title: "Listbox",
  description:
    "A widget that allows users to select one or more items from a list of choices.",
};
