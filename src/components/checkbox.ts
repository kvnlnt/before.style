import type { DocumentedComponent } from "../types";

export const checkbox: DocumentedComponent = {
  title: "Checkbox",
  description:
    "A form control that allows users to select one or more options from a set.",
};
