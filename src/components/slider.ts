import type { DocumentedComponent } from "../types";

export const slider: DocumentedComponent = {
  title: "Slider",
  description:
    "An input control that allows users to select a value from a range by dragging a thumb.",
};
