import type { DocumentedComponent } from "../types";

export const carousel: DocumentedComponent = {
  title: "Carousel",
  description:
    "A rotating set of content items that can be navigated through sequentially.",
};
