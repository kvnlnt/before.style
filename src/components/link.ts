import type { DocumentedComponent } from "../types";

export const link: DocumentedComponent = {
  title: "Link",
  description:
    "An interactive reference to a resource that navigates the user when activated.",
};
