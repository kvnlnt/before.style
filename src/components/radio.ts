import type { DocumentedComponent } from "../types";

export const radio: DocumentedComponent = {
  title: "Radio",
  description:
    "A group of mutually exclusive options where only one can be selected at a time.",
};
