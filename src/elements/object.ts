import type { DocumentedElement } from "../types";

export const object: DocumentedElement = {
  title: "object",
  description:
    "Represents an external resource such as an image, plugin, or nested browsing context.",
};
