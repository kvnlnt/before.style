import type { DocumentedElement } from "../types";

export const template: DocumentedElement = {
  title: "template",
  description:
    "Holds HTML content that is not rendered but can be instantiated via JavaScript.",
};
