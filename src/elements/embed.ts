import type { DocumentedElement } from "../types";

export const embed: DocumentedElement = {
  title: "embed",
  description:
    "Embeds external content from an external application or plugin.",
};
