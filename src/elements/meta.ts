import type { DocumentedElement } from "../types";

export const meta: DocumentedElement = {
  title: "meta",
  description:
    "Represents metadata that cannot be expressed with other HTML elements.",
};
